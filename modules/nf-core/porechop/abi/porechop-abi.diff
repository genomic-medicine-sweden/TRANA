Changes in component 'nf-core/porechop/abi'
'modules/nf-core/porechop/abi/environment.yml' is unchanged
Changes in 'porechop/abi/main.nf':
--- modules/nf-core/porechop/abi/main.nf
+++ modules/nf-core/porechop/abi/main.nf
@@ -5,7 +5,7 @@
     conda "${moduleDir}/environment.yml"
     container "${ workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container ?
         'https://depot.galaxyproject.org/singularity/porechop_abi:0.5.0--py310h590eda1_0':
-        'biocontainers/porechop_abi:0.5.0--py310h590eda1_0' }"
+        'quay.io/biocontainers/porechop_abi:0.5.0--py310h590eda1_0' }"
 
     input:
     tuple val(meta), path(reads)
@@ -20,14 +20,13 @@
     task.ext.when == null || task.ext.when
 
     script:
+    // remove _ if args will be used
     def args = task.ext.args ?: ''
     def prefix = task.ext.prefix ?: "${meta.id}.porechop_abi"
-    def adapters_list = custom_adapters ? "--custom_adapters ${custom_adapters}" : ""
     if ("$reads" == "${prefix}.fastq.gz") error "Input and output names are the same, use \"task.ext.prefix\" to disambiguate!"
     """
     porechop_abi \\
         --input $reads \\
-        $adapters_list \\
         --threads $task.cpus \\
         $args \\
         --output ${prefix}.fastq.gz \\
@@ -39,7 +38,7 @@
     """
 
     stub:
-    def args = task.ext.args ?: ''
+    def _args = task.ext.args ?: ''
     def prefix = task.ext.prefix ?: "${meta.id}.porechop_abi"
     def adapters_list = custom_adapters ? "--custom_adapters ${custom_adapters}" : ""
     """

'modules/nf-core/porechop/abi/meta.yml' is unchanged
'modules/nf-core/porechop/abi/tests/main.nf.test.snap' is unchanged
'modules/nf-core/porechop/abi/tests/main.nf.test' is unchanged
************************************************************
